configuration:
  events:
    recordTaskInVars: false
    recordTaskOutVars: false
  runtime: "concord-v2"

flows:
  default:
    - task: http
      in:
        url: https://googl.com
      out: myResult

    - log: ${myResult}
